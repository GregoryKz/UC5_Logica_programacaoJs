// Enunciado: Calcular o dobro de um número.
// Regras: Dica: multiplicar por 2.

const prompt = require("prompt-sync")();

let numero = Number(prompt("Digite um número: "));

console.log(`Dobro: ${numero * 2}`);
